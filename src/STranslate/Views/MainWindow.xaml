<Window
    x:Class="STranslate.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cnvt="clr-namespace:STranslate.Converters"
    xmlns:control="clr-namespace:STranslate.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:ikw="http://schemas.inkore.net/lib/ui/wpf"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:s="https://github.com/zggsong/2022/xaml"
    xmlns:tb="http://www.hardcodet.net/taskbar"
    xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
    xmlns:vm="clr-namespace:STranslate.ViewModels"
    Name="STWindow"
    Title="STranslate"
    Width="{Binding Settings.MainWindowWidth, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
    MinWidth="400"
    MinHeight="200"
    MaxHeight="{Binding MainWindowEffectiveMaxHeight, Mode=OneWay}"
    control:SizeToContentBehavior.PersistentSizeToContent="Height"
    control:SnackbarBehavior.EnableSnackbar="True"
    d:DataContext="{d:DesignInstance Type=vm:MainWindowViewModel}"
    d:DesignHeight="800"
    d:DesignWidth="470"
    ui:TitleBar.CloseButtonAvailability="Collapsed"
    ui:TitleBar.ExtendViewIntoTitleBar="True"
    ui:TitleBar.MaximizeButtonAvailability="Collapsed"
    ui:TitleBar.MinimizeButtonAvailability="Collapsed"
    ui:WindowHelper.SystemBackdropType="Mica"
    ui:WindowHelper.UseModernWindowStyle="True"
    Closed="OnClosed"
    Icon="/Resources/app.ico"
    Left="{Binding Settings.MainWindowLeft, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
    Loaded="OnLoaded"
    ShowInTaskbar="False"
    Top="{Binding Settings.MainWindowTop, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
    Topmost="{Binding IsTopmost, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
    Visibility="Visible"
    WindowStartupLocation="Manual"
    mc:Ignorable="d">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="SizeChanged">
            <i:InvokeCommandAction Command="{Binding AdjustPositionForContentSizeChangedCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Window.InputBindings>
        <KeyBinding
            Key="Esc"
            Command="{Binding CancelCommand}"
            CommandParameter="{Binding ElementName=STWindow}" />
        <KeyBinding
            Key="Q"
            Command="{Binding ExitCommand}"
            Modifiers="Ctrl+Shift" />
        <KeyBinding
            Key="{Binding HotkeySettings.OpenSettingsHotkey.Key, Converter={cnvt:StringToKeyBindingConverter}, ConverterParameter={x:Static cnvt:HotkeyMode.Key}}"
            Command="{Binding OpenSettingsCommand}"
            Modifiers="{Binding HotkeySettings.OpenSettingsHotkey.Key, Converter={cnvt:StringToKeyBindingConverter}, ConverterParameter={x:Static cnvt:HotkeyMode.Modifiers}}" />
        <KeyBinding
            Key="{Binding HotkeySettings.ToggleColorThemeHotkey.Key, Converter={cnvt:StringToKeyBindingConverter}, ConverterParameter={x:Static cnvt:HotkeyMode.Key}}"
            Command="{Binding ChangeColorSchemeCommand}"
            Modifiers="{Binding HotkeySettings.ToggleColorThemeHotkey.Key, Converter={cnvt:StringToKeyBindingConverter}, ConverterParameter={x:Static cnvt:HotkeyMode.Modifiers}}" />
        <KeyBinding
            Key="{Binding HotkeySettings.ToggleTopmostHotkey.Key, Converter={cnvt:StringToKeyBindingConverter}, ConverterParameter={x:Static cnvt:HotkeyMode.Key}}"
            Command="{Binding ToggleTopmostCommand}"
            Modifiers="{Binding HotkeySettings.ToggleTopmostHotkey.Key, Converter={cnvt:StringToKeyBindingConverter}, ConverterParameter={x:Static cnvt:HotkeyMode.Modifiers}}" />
        <KeyBinding
            Key="{Binding HotkeySettings.HideInputHotkey.Key, Converter={cnvt:StringToKeyBindingConverter}, ConverterParameter={x:Static cnvt:HotkeyMode.Key}}"
            Command="{Binding ToggleHideInputCommand}"
            Modifiers="{Binding HotkeySettings.HideInputHotkey.Key, Converter={cnvt:StringToKeyBindingConverter}, ConverterParameter={x:Static cnvt:HotkeyMode.Modifiers}}" />
        <KeyBinding
            Key="{Binding HotkeySettings.SaveToVocabularyHotkey.Key, Converter={cnvt:StringToKeyBindingConverter}, ConverterParameter={x:Static cnvt:HotkeyMode.Key}}"
            Command="{Binding SaveToVocabularyCommand}"
            CommandParameter="{Binding InputText}"
            Modifiers="{Binding HotkeySettings.SaveToVocabularyHotkey.Key, Converter={cnvt:StringToKeyBindingConverter}, ConverterParameter={x:Static cnvt:HotkeyMode.Modifiers}}" />
        <KeyBinding
            Key="{Binding HotkeySettings.OpenHistoryHotkey.Key, Converter={cnvt:StringToKeyBindingConverter}, ConverterParameter={x:Static cnvt:HotkeyMode.Key}}"
            Command="{Binding OpenHistoryCommand}"
            CommandParameter="{Binding InputText}"
            Modifiers="{Binding HotkeySettings.OpenHistoryHotkey.Key, Converter={cnvt:StringToKeyBindingConverter}, ConverterParameter={x:Static cnvt:HotkeyMode.Modifiers}}" />
        <KeyBinding
            Key="{Binding HotkeySettings.HistoryNavigePreviousHotkey.Key, Converter={cnvt:StringToKeyBindingConverter}, ConverterParameter={x:Static cnvt:HotkeyMode.Key}}"
            Command="{Binding HistoryPreviousCommand}"
            CommandParameter="{Binding InputText}"
            Modifiers="{Binding HotkeySettings.HistoryNavigePreviousHotkey.Key, Converter={cnvt:StringToKeyBindingConverter}, ConverterParameter={x:Static cnvt:HotkeyMode.Modifiers}}" />
        <KeyBinding
            Key="{Binding HotkeySettings.HistoryNavigeNextHotkey.Key, Converter={cnvt:StringToKeyBindingConverter}, ConverterParameter={x:Static cnvt:HotkeyMode.Key}}"
            Command="{Binding HistoryNextCommand}"
            CommandParameter="{Binding InputText}"
            Modifiers="{Binding HotkeySettings.HistoryNavigeNextHotkey.Key, Converter={cnvt:StringToKeyBindingConverter}, ConverterParameter={x:Static cnvt:HotkeyMode.Modifiers}}" />
        <KeyBinding
            Key="{Binding HotkeySettings.AutoTranslateHotkey.Key, Converter={cnvt:StringToKeyBindingConverter}, ConverterParameter={x:Static cnvt:HotkeyMode.Key}}"
            Command="{Binding ToggleAutoTranslateCommand}"
            Modifiers="{Binding HotkeySettings.AutoTranslateHotkey.Key, Converter={cnvt:StringToKeyBindingConverter}, ConverterParameter={x:Static cnvt:HotkeyMode.Modifiers}}" />
    </Window.InputBindings>
    <Grid>
        <control:HeaderControl
            Padding="6,4,6,0"
            VerticalAlignment="Top"
            ColorSchemeCommand="{Binding ChangeColorSchemeCommand}"
            HistoryNextCommand="{Binding HistoryNextCommand}"
            HistoryPreviousCommand="{Binding HistoryPreviousCommand}"
            ImageTranslateCommand="{Binding ImageTranslateCommand}"
            IsAutoTranslate="{Binding Settings.AutoTranslate}"
            IsAutoTranslateVisible="{Binding Settings.IsAutoTranslateVisible}"
            IsClipboardMonitorVisible="{Binding Settings.IsClipboardMonitorVisible}"
            IsClipboardMonitoring="{Binding IsClipboardMonitoring}"
            IsColorSchemeVisible="{Binding Settings.IsColorSchemeVisible}"
            IsHideInput="{Binding Settings.HideInput}"
            IsHideInputVisible="{Binding Settings.IsHideInputVisible}"
            IsHistoryNavigationVisible="{Binding Settings.IsHistoryNavigationVisible}"
            IsImageTranslateVisible="{Binding Settings.IsImageTranslateVisible}"
            IsMouseHook="{Binding IsMouseHook}"
            IsMouseHookVisible="{Binding Settings.IsMouseHookVisible}"
            IsOcrVisible="{Binding Settings.IsOcrVisible}"
            IsScreenshotTranslateVisible="{Binding Settings.IsScreenshotTranslateVisible}"
            IsTopmost="{Binding IsTopmost}"
            OcrCommand="{Binding OcrCommand}"
            ScreenshotTranslateCommand="{Binding ScreenshotTranslateCommand}"
            SettingCommand="{Binding OpenSettingsCommand}"
            VisibleActions="{Binding Settings.MainHeaderVisibleActions}" />

        <ui:ScrollViewerEx Margin="10,37,0,5">
            <StackPanel>
                <!--  //  InputControl  //  -->
                <control:InputControl
                    x:Name="PART_Input"
                    Margin="0,0,10,10"
                    CopyCommand="{Binding CopyCommand}"
                    CurrentFontSize="{Binding Settings.FontSize}"
                    ExecuteCommand="{Binding TranslateCommand}"
                    ForceExecuteCommand="{Binding TranslateCommand}"
                    HasActivedVocabulary="{Binding VocabularyService.HasActivedVocabulary}"
                    CanSelectIdentifiedLanguage="{Binding CanSelectIdentifiedLanguage}"
                    IdentifiedLanguage="{Binding IdentifiedLanguage}"
                    IdentifiedLanguageItemsSource="{Binding IdentifiedLanguageOptions}"
                    IsIdentify="{Binding IsIdentifyProcessing}"
                    PlayAudioCancelCommand="{Binding PlayAudioCancelCommand}"
                    PlayAudioCommand="{Binding PlayAudioCommand}"
                    RemoveLineBreaksCommand="{Binding RemoveLineBreaksCommand}"
                    RemoveSpacesCommand="{Binding RemoveSpacesCommand}"
                    SaveToVocabularyCommand="{Binding SaveToVocabularyCommand}"
                    SelectIdentifiedLanguageCommand="{Binding SelectIdentifiedLanguageCommand}"
                    SelectedIdentifiedLanguage="{Binding SelectedIdentifiedLanguage}"
                    Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}"
                    TranslateOnPaste="{Binding Settings.TranslateOnPaste}"
                    Visibility="{Binding Settings.HideInput, Converter={cnvt:BoolToVisibilityInverseConverter}}" />

                <!--  // LangSelectControl //  -->
                <Grid Margin="0,0,10,10">
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Style.Triggers>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding Settings.HideInput}" Value="True" />
                                        <Condition Binding="{Binding Settings.HideInputWithLangSelectControl}" Value="True" />
                                    </MultiDataTrigger.Conditions>
                                    <MultiDataTrigger.Setters>
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </MultiDataTrigger.Setters>
                                </MultiDataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <ComboBox
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Center"
                        DisplayMemberPath="Display"
                        ItemsSource="{Binding DataProvider.LangEnums}"
                        SelectedValue="{Binding Settings.SourceLang}"
                        SelectedValuePath="Value" />

                    <Button
                        Grid.Column="1"
                        Margin="2,0"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Stretch"
                        Command="{Binding SwapLanguageCommand}"
                        ToolTip="{DynamicResource SwapLanguage}">
                        <ui:FontIcon FontSize="{DynamicResource STControlFontSize16}" Icon="{x:Static ui:FluentSystemIcons.ArrowSwap_20_Regular}" />
                    </Button>

                    <ComboBox
                        Grid.Column="2"
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Center"
                        DisplayMemberPath="Display"
                        ItemsSource="{Binding DataProvider.LangEnums}"
                        SelectedValue="{Binding Settings.TargetLang}"
                        SelectedValuePath="Value" />

                    <ListBox
                        Grid.Column="3"
                        Margin="2,0,0,0"
                        Background="Transparent"
                        ItemsSource="{Binding TranslateService.Services}">
                        <ListBox.Resources>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <ContentPresenter />
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.Resources>
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel VerticalAlignment="Center" Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Button
                                    Padding="0"
                                    Command="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=DataContext.TemporaryTranslateCommand}"
                                    CommandParameter="{Binding}"
                                    ToolTip="{Binding DisplayName}">
                                    <Button.Visibility>
                                        <MultiBinding Converter="{cnvt:ServicePinnedVisibilityConverter}">
                                            <Binding Path="IsEnabled" />
                                            <Binding Path="Options.ExecMode" />
                                            <Binding Path="Options.TemporaryDisplay" />
                                        </MultiBinding>
                                    </Button.Visibility>
                                    <Button.Resources>
                                        <s:BindingProxy x:Key="SvcProxy" Data="{Binding}" />
                                    </Button.Resources>
                                    <Button.ContextMenu>
                                        <ContextMenu>
                                            <MenuItem
                                                Header="{DynamicResource TransExecAutomatic}"
                                                IsCheckable="True"
                                                IsChecked="{Binding Source={StaticResource SvcProxy}, Path=Data.Options.ExecMode, Converter={cnvt:ExecutionModeBoolConverter}, ConverterParameter=Automatic}" />
                                            <MenuItem
                                                Header="{DynamicResource TransExecManual}"
                                                IsCheckable="True"
                                                IsChecked="{Binding Source={StaticResource SvcProxy}, Path=Data.Options.ExecMode, Converter={cnvt:ExecutionModeBoolConverter}, ConverterParameter=Manual}" />
                                            <MenuItem
                                                Header="{DynamicResource TransExecPinned}"
                                                IsCheckable="True"
                                                IsChecked="{Binding Source={StaticResource SvcProxy}, Path=Data.Options.ExecMode, Converter={cnvt:ExecutionModeBoolConverter}, ConverterParameter=Pinned}" />
                                        </ContextMenu>
                                    </Button.ContextMenu>
                                    <Image
                                        Height="{DynamicResource STControlFontSize18}"
                                        Margin="6"
                                        Source="{Binding MetaData.IconPath}" />
                                </Button>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>

                <!--  // OutputControl //  -->
                <control:OutputControl
                    Margin="0,0,10,0"
                    CleanTransBackCommand="{Binding CleanTransBackCommand}"
                    CopyCamelCaseCommand="{Binding CopyCamelCaseCommand}"
                    CopyCommand="{Binding CopyCommand}"
                    CopyPascalCaseCommand="{Binding CopyPascalCaseCommand}"
                    CopySnakeCaseCommand="{Binding CopySnakeCaseCommand}"
                    ExplainCommand="{Binding ExplainCommand}"
                    HasActivedVocabulary="{Binding VocabularyService.HasActivedVocabulary}"
                    InsertCommand="{Binding InsertCommand}"
                    ItemsSource="{Binding TranslateService.Services}"
                    NavigateCommand="{Binding NavigateCommand}"
                    PlayAudioCancelCommand="{Binding PlayAudioCancelCommand}"
                    PlayAudioCommand="{Binding PlayAudioCommand}"
                    PlayAudioUrlCommand="{Binding PlayAudioUrlCommand}"
                    RetryCommand="{Binding SingleTranslateCommand}"
                    SaveToVocabularyWithNoteCommand="{Binding SaveToVocabularyWithNoteCommand}"
                    ShowBackTranslation="{Binding Settings.ShowBackTranslation}"
                    ShowCamelCase="{Binding Settings.ShowCamelCase}"
                    ShowInsert="{Binding Settings.ShowInsert}"
                    ShowPascalCase="{Binding Settings.ShowPascalCase}"
                    ShowPrompt="{Binding Settings.ShowPromptButton}"
                    ShowSnakeCase="{Binding Settings.ShowSnakeCase}"
                    TransBackCommand="{Binding SingleTransBackCommand}" />
            </StackPanel>
        </ui:ScrollViewerEx>

        <!--#region Tray Icon-->

        <tb:TaskbarIcon
            x:Name="PART_NotifyIcon"
            DoubleClickCommand="{Binding DoubleClickCommand}"
            IconSource="{Binding TrayIcon}"
            LeftClickCommand="{Binding LeftClickCommand}"
            NoLeftClickDelay="True"
            ToolTipText="{Binding TrayToolTip}"
            Visibility="{Binding Settings.HideNotifyIcon, Converter={cnvt:BoolToVisibilityInverseConverter}}">
            <tb:TaskbarIcon.Resources>
                <s:BindingProxy x:Key="ViewModel" Data="{Binding}" />
            </tb:TaskbarIcon.Resources>
            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu>
                    <MenuItem Command="{Binding Source={StaticResource ViewModel}, Path=Data.ToggleAppCommand}">
                        <MenuItem.Header>
                            <Grid>
                                <TextBlock Text="{DynamicResource Open}" Visibility="{Binding Settings.DisableGlobalHotkeys, Converter={cnvt:BoolToVisibilityConverter}}" />
                                <TextBlock Visibility="{Binding Settings.DisableGlobalHotkeys, Converter={cnvt:BoolToVisibilityInverseConverter}}">
                                    <Run Text="{DynamicResource Open}" />
                                    <Run Text="(" />
                                    <Run Text="{Binding Source={StaticResource ViewModel}, Path=Data.HotkeySettings.OpenWindowHotkey.Key}" />
                                    <Run Text=")" />
                                </TextBlock>
                            </Grid>
                        </MenuItem.Header>
                        <MenuItem.Icon>
                            <ui:FontIcon Icon="{x:Static ui:FluentSystemIcons.WindowAd_20_Regular}" />
                        </MenuItem.Icon>
                    </MenuItem>

                    <MenuItem
                        Command="{Binding Source={StaticResource ViewModel}, Path=Data.ScreenshotTranslateCommand}"
                        Header="{DynamicResource Hotkey_ScreenshotTranslate}"
                        Visibility="{Binding Source={StaticResource ViewModel}, Path=Data.Settings.ShowScreenshotItemInNotifyIconMenu, Converter={cnvt:BoolToVisibilityConverter}}">
                        <MenuItem.Icon>
                            <ui:FontIcon Icon="{x:Static ui:FluentSystemIcons.ScreenCut_20_Regular}" />
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem
                        Command="{Binding Source={StaticResource ViewModel}, Path=Data.ImageTranslateCommand}"
                        Header="{DynamicResource ImageTranslate}"
                        Visibility="{Binding Source={StaticResource ViewModel}, Path=Data.Settings.ShowImageTranslateItemInNotifyIconMenu, Converter={cnvt:BoolToVisibilityConverter}}">
                        <MenuItem.Icon>
                            <ui:FontIcon Icon="{x:Static ui:FluentSystemIcons.DrawImage_20_Regular}" />
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem
                        Command="{Binding Source={StaticResource ViewModel}, Path=Data.OcrCommand}"
                        Header="{DynamicResource Ocr}"
                        Visibility="{Binding Source={StaticResource ViewModel}, Path=Data.Settings.ShowOcrItemInNotifyIconMenu, Converter={cnvt:BoolToVisibilityConverter}}">
                        <MenuItem.Icon>
                            <ui:FontIcon Icon="{x:Static ui:FluentSystemIcons.ScanType_20_Regular}" />
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem
                        Command="{Binding Source={StaticResource ViewModel}, Path=Data.QrCodeCommand}"
                        Header="{DynamicResource QrCode}"
                        Visibility="{Binding Source={StaticResource ViewModel}, Path=Data.Settings.ShowQrCodeItemInNotifyIconMenu, Converter={cnvt:BoolToVisibilityConverter}}">
                        <MenuItem.Icon>
                            <ui:FontIcon Icon="{x:Static ui:FluentSystemIcons.QrCode_20_Regular}" />
                        </MenuItem.Icon>
                    </MenuItem>

                    <MenuItem Command="{Binding Source={StaticResource ViewModel}, Path=Data.ResetLocationCommand}" Header="{DynamicResource ResetLocation}">
                        <MenuItem.Icon>
                            <ui:FontIcon Icon="{x:Static ui:FluentSystemIcons.ArrowMinimize_20_Regular}" />
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem
                        Command="{Binding Source={StaticResource ViewModel}, Path=Data.OpenSettingsCommand}"
                        CommandParameter=""
                        Header="{DynamicResource Settings}">
                        <MenuItem.Icon>
                            <ui:FontIcon Icon="{x:Static ui:FluentSystemIcons.Settings_20_Regular}" />
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Command="{Binding Source={StaticResource ViewModel}, Path=Data.ExitCommand}" Header="{DynamicResource Exit}">
                        <MenuItem.Icon>
                            <ui:FontIcon Icon="{x:Static ui:FluentSystemIcons.ArrowExit_20_Regular}" />
                        </MenuItem.Icon>
                    </MenuItem>
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>
        </tb:TaskbarIcon>

        <!--#endregion-->
    </Grid>
</Window>
